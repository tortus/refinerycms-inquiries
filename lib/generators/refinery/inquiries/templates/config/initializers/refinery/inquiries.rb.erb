Refinery::Inquiries.configure do |config|
  # Configure whether to show privacy link
  # config.show_contact_privacy_link = <%= Refinery::Inquiries.show_contact_privacy_link.inspect %>

  # Configure custom attributes (saved in serialized "custom_store" column).
  # You may use this format (all settings are completely optional):
  #
  # config.custom_inquiry_attributes = {
  #   :my_attribute => {
  #     :default => "hello",
  #     :label => "My Attribute"
  #     :placeholder => "...",
  #     :validates => {...}
  #   },
  #   :another_attribute => {...}
  # }
  #
  # The :validates hash will be passed as the second argument to "validates :my_attribute, ..."
  #
  # config.custom_inquiry_attributes = <%= Refinery::Inquiries.custom_inquiry_attributes.inspect %>

  # Configure whether inquiries marked as spam should also send a notification mail
  # config.send_notifications_for_inquiries_marked_as_spam = <%= Refinery::Inquiries.send_notifications_for_inquiries_marked_as_spam.inspect %>

  # Add extra spam keywords
  # config.extra_spam_words = %w()

  # Enable Recaptcha gem on inquiry form. Requires recaptcha gem, see: https://github.com/ambethia/recaptcha
  # config.use_recaptcha = <%= Refinery::Inquiries.use_recaptcha %>

  # Require the form to be submitted via JavaScript (may reduce spam at cost of blocking NoScript users).
  # config.submit_via_javascript = <%= Refinery::Inquiries.submit_via_javascript %>

  # Add a hidden honeypot "name" field for spam bots that should be left blank.
  # config.use_honeypot = <%= Refinery::Inquiries.use_honeypot %>
  # config.honeypot_field_name = :<%= Refinery::Inquiries.honeypot_field_name %>
end
